# nutrai
An AI powered health and nutrition recommendation assistant.

Run `npm install` to install the required packages. 
Run `node app.js` to run the NodeJS backend, and then`npm start` to run the React frontend. 
